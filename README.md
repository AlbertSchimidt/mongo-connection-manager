# mongo-connection-manager
A lib Ive made to lessen the copy paste of mongo configuration classes and to centralize the URI properties of the database. Auto creates and injects mongo templates based on application configuration and lib configuration. It will use the same external configuration context of the application but can be overridden if necessary
